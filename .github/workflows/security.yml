name: Security

on:
  # Per-PR security runs in ci.yml's "Quality Gate" job; this is the deeper weekly sweep.
  schedule:
    - cron: "0 6 * * 1"   # weekly Monday 06:00 UTC
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  scan:
    name: Bandit · pip-audit · secrets
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - name: Install security tools
        run: |
          python -m pip install --upgrade pip
          pip install bandit[toml] pip-audit detect-secrets

      - name: Bandit (SAST)
        run: bandit -c pyproject.toml -r src

      - name: detect-secrets (audit tracked files against baseline)
        run: git ls-files -z | xargs -0 detect-secrets-hook --baseline .secrets.baseline

      - name: pip-audit (dependency CVEs)
        # PYSEC-2026-311: ChromaDB server-mode pre-auth RCE (/api/v2 + trust_remote_code). Not
        # reachable here — we use embedded chromadb.EphemeralClient (no HTTP server). No fixed
        # release exists yet (latest 1.5.9 still affected). Revisit when a patch ships.
        run: pip-audit -r requirements.txt --strict --ignore-vuln PYSEC-2026-311 || echo "::warning::pip-audit found advisories — review above"
