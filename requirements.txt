# ── Core pipeline (per IMPLEMENTATION_CONTEXT.md §5; dissertation v6) ─────────
langgraph>=1.0          # v1.0+ state machine (dissertation p.39)
langchain
langchain-openai
langchain-community
langchain-ollama

# ── Knowledge / RAG ─────────────────────────────────────────────────────────
# graphrag deferred to Card 10: it pins numpy~=2.1, which conflicts with the scispaCy-driven
# numpy<2 set (Card 5). Re-add (isolated / version-resolved) when GraphRAG lands.
chromadb
neo4j

# ── NLP / NER ───────────────────────────────────────────────────────────────
scispacy==0.6.2          # numpy-1-era; coheres with transformers 5.x on numpy 1.26
spacy>=3.7,<3.8          # pinned by the en_core_sci_* model (Card 5)
numpy>=1.26,<2           # coherent set: scispaCy/spaCy/scipy/transformers all on numpy 1.26
sentencepiece            # tokenizer backend for medical LLMs
# scispaCy model installed separately: see requirements-models.txt

# ── Vision-language image embeddings (BioViL-T) ──────────────────────────────
hi-ml-multimodal         # Microsoft health_multimodal — BioViL/BioViL-T (Boecking et al.)
# NOTE: install CUDA torch for GPU (RTX 5070 / Blackwell):
#   pip install --index-url https://download.pytorch.org/whl/cu128 torch torchvision

# ── ML / vision-language / embeddings ───────────────────────────────────────
transformers
torch
accelerate
bitsandbytes            # 4-bit quantisation (LLaVA-Med / BioViL on 8 GB VRAM)
sentence-transformers
pillow                  # CXR image IO
# LLaVA-Med 7B (VLM) + BioViL (image embeddings) pulled from HF during Card 3/4.

# ── Argumentation / data ────────────────────────────────────────────────────
networkx
pandas
numpy
scikit-learn
kagglehub               # fetch the NIH ChestX-ray14 sample slice (Track-1 surrogate)

# ── UI / tracking ───────────────────────────────────────────────────────────
streamlit
graphviz
mlflow

# ── LLM serving (local) ─────────────────────────────────────────────────────
ollama
