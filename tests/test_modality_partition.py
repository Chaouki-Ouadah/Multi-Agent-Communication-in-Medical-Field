"""Modality partitioner tests: per-agent views disjoint over raw data; no cross-modality leakage.

TDD placeholder — replace with failing tests when implementing (IMPLEMENTATION_CONTEXT.md Card 2)."""

import pytest

pytestmark = pytest.mark.skip(reason="not implemented yet — see docs/plans/cards.md")


def test_placeholder() -> None:
    raise NotImplementedError
