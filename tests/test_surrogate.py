"""Surrogate generator + loader tests: shape, missingness, ground-truth labels.

TDD placeholder — replace with failing tests when implementing (IMPLEMENTATION_CONTEXT.md §6.2)."""

import pytest

pytestmark = pytest.mark.skip(
    reason="not implemented yet — see IMPLEMENTATION_CONTEXT.md build order"
)


def test_placeholder() -> None:
    raise NotImplementedError
