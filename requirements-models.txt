# Large model downloads — installed separately (not on every CI run).
# scispaCy clinical NER model. `en_core_sci_sm` is used for dev/tests (small, fast);
# swap to `en_core_sci_lg` for production via SCISPACY_MODEL. Works with scispaCy 0.6.2 + spaCy 3.7.
https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.4/en_core_sci_sm-0.5.4.tar.gz
