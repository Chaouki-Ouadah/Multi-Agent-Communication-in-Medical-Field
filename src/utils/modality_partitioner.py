"""Modality partitioner: project a Case into per-agent views (vision/report/clinical/supervisor). Enforces OIDP.

Stub — no logic yet. Implement per IMPLEMENTATION_CONTEXT.md §3, Card 2 (TDD: write the failing test first).
"""
