"""Supervisor agent: sees only the three agents' text arguments (never raw data); mediates debate, detects cross-modal conflict, manages convergence (<=5 rounds).

Stub — no logic yet. Implement per IMPLEMENTATION_CONTEXT.md §3, Card 7 (TDD: write the failing test first).
"""
